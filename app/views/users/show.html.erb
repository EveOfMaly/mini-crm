Change layout


<div class="table">
    <div class="table-wrapper">
    <table>
    <thead>
        <tr>
        <th></th>
        <th>Name</th>
        <th>Age</th>
        <th>Email</th>
        <th>Gender</th>
        <th>Last Visit</th>
        <th>Region</th>
        <th>City</th>
        <th>Country Code</th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user|  %>
        <tr>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
            <td><%= user.username %></td>
        </tr>
        <% end %>
    </tbody>
    </table>
  </div>
</div>



<%= button_to('Delete User', user_path, method: :delete) %> <br>
<% if logged_in? == true %>
<%= link_to "Create a page",  user_path %>
<% end %>

